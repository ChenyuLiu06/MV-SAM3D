# MV-SAM3D 运行说明

这份文档记录当前 `gpu-230` 上已经验证过的运行环境、权重位置、输入格式和最小运行命令。以后跑测试时先看这里，不需要反复翻代码。

## 1. 机器、仓库和 Python 环境

运行机器：

- SSH 别名：`230_e0`
- 主机：`gpu-230`
- GPU：NVIDIA GeForce RTX 5090，32 GB 显存
- MV-SAM3D 仓库：`/home/yilin/chenyu/MV-SAM3D`
- 共用环境：`/mnt/conda/yilin/sam3d_5090`
- Python：`/mnt/conda/yilin/sam3d_5090/bin/python`

登录并进入仓库：

```bash
ssh 230_e0
cd /home/yilin/chenyu/MV-SAM3D
```

当前已确认的主要版本：

```text
Python       3.11.0
PyTorch      2.7.1+cu128
CUDA         12.8.93
PyTorch3D    0.7.8（sm_120 / RTX 5090）
Kaolin       0.18.0
xFormers     0.0.31.post1，但运行时禁用
```

检查 GPU 和 Python：

```bash
nvidia-smi
/mnt/conda/yilin/sam3d_5090/bin/python -c "import torch; print(torch.__version__, torch.cuda.is_available(), torch.cuda.get_device_name(0))"
```

## 2. 权重和缓存位置

### 2.1 MV-SAM3D pipeline 权重

MV-SAM3D 当前从仓库下的 `checkpoints` 入口读取权重：

```text
/home/yilin/chenyu/MV-SAM3D/checkpoints
```

目前这个目录指向 5090 兼容的权重目录：

```text
/home/yilin/chenyu/sam-3d-objects-5090/checkpoints
```

实际使用的配置是：

```text
/home/yilin/chenyu/MV-SAM3D/checkpoints/hf-5090/pipeline.yaml
```

检查入口是否正常：

```bash
readlink -f /home/yilin/chenyu/MV-SAM3D/checkpoints
test -f /home/yilin/chenyu/MV-SAM3D/checkpoints/hf-5090/pipeline.yaml && echo OK
```

### 2.2 SAM3D 共用权重

pipeline 依赖的 Hugging Face 权重目录：

```text
/home/ZhexiLuo/chenyu/weights/sam3d_shared/hf
```

其中应能找到这些核心文件（文件名可能随上游版本略有变化，缺失时先检查目录）：

```text
ss_generator.ckpt
ss_decoder.ckpt
slat_generator.ckpt
slat_decoder_mesh.ckpt
slat_decoder_gs.ckpt
slat_decoder_gs_4.ckpt
```

### 2.3 MOGE 和 DINO 缓存

当前最小测试使用仓库内部的 MOGE 深度/点图估计，MOGE 权重为：

```text
/home/ZhexiLuo/chenyu/weights/moge-vitl/model.pt
```

DINOv2 权重缓存为：

```text
/home/yilin/.cache/torch/hub/checkpoints/dinov2_vitl14_reg4_pretrain.pth
```

当前这条最小复现路线不需要 DA3，也不读取外部真实 pointmap、相机内参或 `pose.yaml`。

## 3. 每次运行前的环境设置

建议每次新开 SSH 会话后执行：

```bash
export MVSAM_REPO=/home/yilin/chenyu/MV-SAM3D
export MVSAM_ENV=/mnt/conda/yilin/sam3d_5090
export GPU_ID=0

export CONDA_PREFIX="$MVSAM_ENV"
export CUDA_HOME="$MVSAM_ENV"
export CUDA_VISIBLE_DEVICES="$GPU_ID"
export LIDRA_SKIP_INIT=true
export XFORMERS_DISABLED=1
export HF_HUB_OFFLINE=1
export PYTHONPATH="$MVSAM_REPO:$MVSAM_REPO/notebook"
export MPLCONFIGDIR=/tmp/yilin-mpl
export TORCH_EXTENSIONS_DIR=/mnt/conda/yilin/torch-extensions
export LD_LIBRARY_PATH="$MVSAM_ENV/targets/x86_64-linux/lib:$MVSAM_ENV/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"

cd "$MVSAM_REPO"
```

说明：

- `GPU_ID=0` 可以改成 `1`、`2` 等空闲 GPU。
- `HF_HUB_OFFLINE=1` 强制使用本机已有权重，避免运行时联网下载。
- `XFORMERS_DISABLED=1` 是 RTX 5090 当前环境的已验证设置；不要自行删掉。
- 同一时刻只建议一个进程占用指定 GPU。

## 4. 最小两视角测试

已经准备好的测试数据位于：

```text
/home/yilin/chenyu/MV-SAM3D/data/dreammanip_test_2v
```

目录结构：

```text
data/dreammanip_test_2v/
├── images/0.png
├── images/1.png
└── object/0.png
    object/1.png
```

其中：

- `images/0.png` 对应 DreamManip 的 `test/view_000/rgb.png`
- `images/1.png` 对应 DreamManip 的 `test/view_001/rgb.png`
- `object/*.png` 是 RGBA mask，物体区域写在 alpha 通道

运行命令：

```bash
cd "$MVSAM_REPO"
"$MVSAM_ENV/bin/python" run_inference.py \
  --input_path "$MVSAM_REPO/data/dreammanip_test_2v" \
  --mask_prompt object \
  --image_names 0,1 \
  --model_tag hf-5090 \
  --decode_formats gaussian,mesh \
  --seed 42 \
  --stage1_steps 50 \
  --stage2_steps 25
```

注意：当前代码的后处理仍会读取 Gaussian 输出，所以即使最终只关心 mesh，也必须写：

```text
--decode_formats gaussian,mesh
```

只写 `--decode_formats mesh` 会在后处理阶段因为缺少 `outputs["gaussian"]` 报 `KeyError`。

## 5. DreamManip 数据的位置和接入关系

原始数据根目录保持不变：

```text
/home/yilin/chenyu/dreammanip2/mvmesh/test
```

单视角目录示例：

```text
/home/yilin/chenyu/dreammanip2/mvmesh/test/view_000/
├── rgb.png
├── sam2_mask.png
├── depth_aligned_m.npy
├── pointmap.npy
└── daid_config.json
```

当前两视角测试只是把 `view_000`、`view_001` 的 RGB 和 SAM2 mask 适配成 MV-SAM3D 输入；最小复现阶段实际点图仍由 pipeline 内部 MOGE 生成。

## 6. 输出位置和文件含义

运行结束后，主要输出在：

```text
/home/yilin/chenyu/MV-SAM3D/visualization/object_0_1/
├── result.glb
└── result.ply
```

- `result.glb`：最终可查看的三角 mesh，包含 geometry、纹理/颜色和当前 pipeline 的尺度结果。当前已验证约 45 万顶点、90 万三角面。
- `result.ply`：Gaussian Splatting 点表示，不是三角面 mesh；不要把它当作最终 mesh 使用。

为方便和 DreamManip 测试目录对照，已复制一份：

```text
/home/yilin/chenyu/dreammanip2/mvmesh/test/mvsam3d_2view_mesh.glb
```

## 7. 快速确认 GLB 确实是三角 mesh

```bash
"$MVSAM_ENV/bin/python" - <<'PY'
import trimesh
path = "/home/yilin/chenyu/MV-SAM3D/visualization/object_0_1/result.glb"
scene = trimesh.load(path, force="scene")
print(scene)
for name, geom in scene.geometry.items():
    print(name, "vertices=", len(geom.vertices), "faces=", len(geom.faces), "visual=", type(geom.visual).__name__)
PY
```

正常情况下应看到至少一个 geometry，且 `faces` 大于 0。

## 8. 当前路线的边界

当前统一脚本已经从 DreamManip 原始数据接入真实 pointmap，但仍保持 MV-SAM3D 原本的多视角生成路线：

- SAM2 负责从 `daid_config.json` 的点标注生成 mask；
- 真实深度和内参生成每个视角的 pointmap；
- 每个视角的 RGB、mask、pointmap 在模型内部同步裁剪、归一化和编码；
- 模型内部做多视角 latent 融合；
- 不做 ICP、PLY 拼接或外部点云配准；
- 不使用 `pose.yaml` 和相机外参参与当前重建；
- 不接入之前的 `fused_object.ply`。

pointmap 输入数组形状为 `(3,H,W)`，采用 OpenCV 相机坐标：`X` 向右、`Y` 向下、`Z` 向前，单位为米。当前 pipeline 内部会做 OpenCV 到 PyTorch3D 的坐标转换 `(x,y,z) -> (-x,-y,z)`，外部不要重复转换。

## 9. 统一批处理入口

脚本位置：

```text
/home/yilin/chenyu/MV-SAM3D/run_dreammanip_mvmesh.sh
```

处理全部实例：

```bash
cd /home/yilin/chenyu/MV-SAM3D
./run_dreammanip_mvmesh.sh 0
```

只处理一个实例：

```bash
./run_dreammanip_mvmesh.sh 0 --instance fuelcan
```

脚本的阶段顺序：

```text
mvmesh/<instance>/view_*/rgb.png + daid_config.json
    ↓
SAM2 mask
    ↓
depth_aligned_m.npy + intrinsic.yaml
    ↓
每视角 pointmap.npy 和运行时输入包
    ↓
MV-SAM3D 多视角推理
    ↓
使用模型预测的 scale / rotation / translation 变换 mesh
    ↓
mvmesh/<instance>/mesh.glb
```

这里的 `--da3_output` 只是当前旧推理脚本承载“外部 pointmap”的参数名，脚本传入的是本地真实深度生成的 `real_pointmaps.npz`，没有运行 DA3。

## 10. 常见问题

### 找不到 checkpoint

```bash
readlink -f /home/yilin/chenyu/MV-SAM3D/checkpoints
ls -lh /home/yilin/chenyu/MV-SAM3D/checkpoints/hf-5090
```

确认 `pipeline.yaml` 和对应 checkpoint 文件存在。

### xFormers 报错或显存行为异常

确认：

```bash
export XFORMERS_DISABLED=1
```

并重新启动 Python 进程。

### 只生成 mesh 报 `KeyError: 'gaussian'`

把参数改为：

```bash
--decode_formats gaussian,mesh
```

### mask 无效或物体没有被识别

`object/*.png` 必须是 RGBA PNG，alpha 通道是二值/灰度物体 mask；仅有 RGB 或单通道 `L` 图不能直接替代当前输入格式。
